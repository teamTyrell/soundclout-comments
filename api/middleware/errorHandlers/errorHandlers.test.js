const errors = require('../../constants/errors');

describe('errorHandlers', () => {

  describe('artistsErrorHandler()', () => {

    it(`should set an '${ errors.ARTIST_NOT_FOUND }' error in the response`, done => {
      done();
    });

  });
});